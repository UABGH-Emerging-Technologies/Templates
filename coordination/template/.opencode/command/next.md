---
description: Board-driven session orientation (user-triggered only — run when invoked, never proactively) — sync the project board to ground truth, process cleared human gates, list the human's queue, pick up the next Todo card
---

Read `process/next.md` in this repository and follow it exactly. It is the canonical, tool-neutral procedure; this file is only the OpenCode entry point. Work-selection filter (step 4): $ARGUMENTS
